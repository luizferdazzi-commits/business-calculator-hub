'use client';import Link from'next/link';import{useMemo,useState}from'react';export default function Page(){const[cash,setCash]=useState('150000'),[revenue,setRevenue]=useState('20000'),[expenses,setExpenses]=useState('35000');const r=useMemo(()=>{const burn=(+expenses||0)-(+revenue||0);return{burn,months:burn>0?(+cash||0)/burn:Infinity}},[cash,revenue,expenses]);const m=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">CASH PLANNING</p><h1>Business Runway Calculator</h1><p className="lead">Estimate how many months your current cash can support the business at its present net burn.</p><div className="grid"><div className="panel"><label>Cash balance<input value={cash} onChange={e=>setCash(e.target.value)}/></label><label>Monthly cash revenue<input value={revenue} onChange={e=>setRevenue(e.target.value)}/></label><label>Monthly cash expenses<input value={expenses} onChange={e=>setExpenses(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Estimated runway</p><div className="big">{Number.isFinite(r.months)?r.months.toFixed(1)+' months':'No net burn'}</div><div className="resultGrid"><div><span>Monthly net burn</span><strong>{m(r.burn)}</strong></div></div></div></div></section><section className="content"><h2>Cash runway formula</h2><p>Runway = cash balance ÷ monthly net burn. Update the inputs as revenue, expenses or financing changes.</p><a className="primary" href="/guides/how-to-calculate-business-runway">Read the guide →</Link></section></main>}