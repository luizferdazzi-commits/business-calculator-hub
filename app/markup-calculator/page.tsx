'use client';
import Link from 'next/link';import{useMemo,useState}from'react';
export default function Page(){const[cost,setCost]=useState('60');const[markup,setMarkup]=useState('50');const r=useMemo(()=>{const c=Number(cost)||0,m=Number(markup)||0;const price=c*(1+m/100),profit=price-c,margin=price>0?profit/price*100:0;return{price,profit,margin}},[cost,markup]);const money=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">FREE PRICING TOOL</p><h1>Markup Calculator</h1><p className="lead">Turn product cost and target markup into a selling price, profit and equivalent profit margin.</p><div className="grid"><div className="panel"><label>Cost<div className="inputWrap"><span>$</span><input inputMode="decimal" value={cost} onChange={e=>setCost(e.target.value)}/></div></label><label>Markup (%)<input inputMode="decimal" value={markup} onChange={e=>setMarkup(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Suggested selling price</p><div className="big">{money(r.price)}</div><div className="resultGrid"><div><span>Profit</span><strong>{money(r.profit)}</strong></div><div><span>Markup</span><strong>{(Number(markup)||0).toFixed(2)}%</strong></div><div><span>Profit margin</span><strong>{r.margin.toFixed(2)}%</strong></div></div></div></div></section><section className="content"><h2>Markup vs. profit margin</h2><p>Markup measures profit relative to cost, while profit margin measures profit relative to selling price. They are related, but the percentages are not interchangeable.</p><div className="relatedBox"><strong>Related pricing tools</strong><div className="relatedLinks"><Link href="/profit-margin-calculator">Profit Margin Calculator →</Link><Link href="/break-even-calculator">Break-Even Calculator →</Link><Link href="/guides/how-to-calculate-profit-margin">Profit Margin Guide →</Link></div></div></section></main>}