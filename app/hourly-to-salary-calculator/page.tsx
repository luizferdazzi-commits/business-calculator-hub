'use client';
import Link from'next/link';import{useMemo,useState}from'react';
export default function Page(){const[rate,setRate]=useState('30');const[hours,setHours]=useState('40');const[weeks,setWeeks]=useState('52');const r=useMemo(()=>{const a=Number(rate)||0,h=Number(hours)||0,w=Number(weeks)||0;const annual=a*h*w;return{annual,monthly:annual/12,weekly:a*h,daily:a*h/5}},[rate,hours,weeks]);const money=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD',maximumFractionDigits:2});return <main className="shell"><section className="calculator"><p className="eyebrow">FREE PAY CALCULATOR</p><h1>Hourly to Salary Calculator</h1><p className="lead">Convert an hourly wage into estimated annual, monthly, weekly and daily pay.</p><div className="grid"><div className="panel"><label>Hourly rate<div className="inputWrap"><span>$</span><input inputMode="decimal" value={rate} onChange={e=>setRate(e.target.value)}/></div></label><label>Hours per week<input inputMode="decimal" value={hours} onChange={e=>setHours(e.target.value)}/></label><label>Paid weeks per year<input inputMode="decimal" value={weeks} onChange={e=>setWeeks(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Estimated annual salary</p><div className="big">{money(r.annual)}</div><div className="resultGrid"><div><span>Monthly</span><strong>{money(r.monthly)}</strong></div><div><span>Weekly</span><strong>{money(r.weekly)}</strong></div><div><span>Daily*</span><strong>{money(r.daily)}</strong></div></div><small>*Daily estimate assumes a five-day workweek.</small></div></div></section><section className="content"><h2>Hourly wage to salary formula</h2><p><strong>Annual Salary = Hourly Rate × Hours per Week × Paid Weeks per Year.</strong> Adjust the inputs to match your actual schedule.</p><div className="relatedBox"><strong>Compare pay another way</strong><div className="relatedLinks"><Link href="/salary-to-hourly-calculator">Salary to Hourly Calculator →</Link><Link href="/freelance-rate-calculator">Freelance Rate Calculator →</Link><Link href="/guides/salary-to-hourly-rate">Salary Conversion Guide →</Link></div></div></section></main>}