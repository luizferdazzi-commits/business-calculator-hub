'use client';import Link from'next/link';import{useMemo,useState}from'react';export default function Page(){const[spend,setSpend]=useState('12000'),[customers,setCustomers]=useState('120'),[revenue,setRevenue]=useState('80'),[margin,setMargin]=useState('70'),[months,setMonths]=useState('24');const r=useMemo(()=>{const cac=(+customers||0)?(+spend||0)/(+customers||1):0,ltv=(+revenue||0)*(+margin||0)/100*(+months||0);return{cac,ltv,ratio:cac?ltv/cac:0}},[spend,customers,revenue,margin,months]);const m=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">UNIT ECONOMICS</p><h1>CAC & LTV Calculator</h1><p className="lead">Estimate customer acquisition cost, customer lifetime value and the LTV:CAC ratio.</p><div className="grid"><div className="panel"><label>Sales & marketing spend<input value={spend} onChange={e=>setSpend(e.target.value)}/></label><label>New customers acquired<input value={customers} onChange={e=>setCustomers(e.target.value)}/></label><label>Monthly revenue/customer<input value={revenue} onChange={e=>setRevenue(e.target.value)}/></label><label>Gross margin (%)<input value={margin} onChange={e=>setMargin(e.target.value)}/></label><label>Average customer lifetime (months)<input value={months} onChange={e=>setMonths(e.target.value)}/></label></div><div className="result"><p className="resultLabel">LTV:CAC ratio</p><div className="big">{r.ratio.toFixed(2)}×</div><div className="resultGrid"><div><span>CAC</span><strong>{m(r.cac)}</strong></div><div><span>LTV</span><strong>{m(r.ltv)}</strong></div></div></div></div></section><section className="content"><h2>Understand unit economics</h2><p>CAC measures acquisition spend per new customer. This LTV estimate uses revenue, gross margin and expected customer lifetime.</p><Link className="primary" href="/guides/cac-vs-ltv-explained">Read the guide →</Link></section></main>}