'use client';
import Link from 'next/link';import{useMemo,useState}from'react';
export default function Page(){const[investment,setInvestment]=useState('5000');const[returned,setReturned]=useState('7500');const r=useMemo(()=>{const i=Number(investment)||0,v=Number(returned)||0;return{profit:v-i,roi:i>0?((v-i)/i)*100:0}},[investment,returned]);const money=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">FREE BUSINESS CALCULATOR</p><h1>ROI Calculator</h1><p className="lead">Calculate return on investment and profit in seconds. Useful for marketing, equipment, software and business projects.</p><div className="grid"><div className="panel"><label>Amount invested<div className="inputWrap"><span>$</span><input inputMode="decimal" value={investment} onChange={e=>setInvestment(e.target.value)}/></div></label><label>Final value / return<div className="inputWrap"><span>$</span><input inputMode="decimal" value={returned} onChange={e=>setReturned(e.target.value)}/></div></label></div><div className="result"><p className="resultLabel">Return on investment</p><div className="big">{r.roi.toFixed(2)}%</div><div className="resultGrid"><div><span>Net profit</span><strong>{money(r.profit)}</strong></div><div><span>Investment</span><strong>{money(Number(investment)||0)}</strong></div><div><span>Final value</span><strong>{money(Number(returned)||0)}</strong></div></div></div></div></section><section className="content"><h2>How ROI is calculated</h2><p><strong>ROI = (Net Profit ÷ Investment Cost) × 100.</strong> A positive result means the return exceeded the original investment; a negative result means a loss based on the values entered.</p><div className="relatedBox"><strong>Keep exploring</strong><div className="relatedLinks"><Link href="/profit-margin-calculator">Profit Margin Calculator →</Link><Link href="/break-even-calculator">Break-Even Calculator →</Link><Link href="/guides">Business Guides →</Link></div></div></section></main>}