'use client';import Link from'next/link';import{useMemo,useState}from'react';export default function Page(){const[cost,setCost]=useState('60'),[margin,setMargin]=useState('40');const r=useMemo(()=>{const c=+cost||0,m=Math.min(99.9,+margin||0)/100,price=c/(1-m);return{price,profit:price-c,markup:c?((price-c)/c*100):0}},[cost,margin]);const f=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">PRICING & MARGIN</p><h1>Selling Price Calculator</h1><p className="lead">Calculate the selling price required to hit a target profit margin from your cost.</p><div className="grid"><div className="panel"><label>Unit cost<input value={cost} onChange={e=>setCost(e.target.value)}/></label><label>Target margin (%)<input value={margin} onChange={e=>setMargin(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Required selling price</p><div className="big">{f(r.price)}</div><div className="resultGrid"><div><span>Profit/unit</span><strong>{f(r.profit)}</strong></div><div><span>Equivalent markup</span><strong>{r.markup.toFixed(2)}%</strong></div></div></div></div></section><section className="content"><h2>Price from cost and margin</h2><p>Selling price = cost ÷ (1 − target margin). Margin and markup are not interchangeable.</p><a className="primary" href="/guides/how-to-set-selling-price-from-cost-and-margin">Read the guide →</a></section></main>}