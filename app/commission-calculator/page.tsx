'use client';
import Link from'next/link';import{useMemo,useState}from'react';
export default function Page(){const[sales,setSales]=useState('10000');const[rate,setRate]=useState('10');const[base,setBase]=useState('0');const r=useMemo(()=>{const s=Number(sales)||0,p=Number(rate)||0,b=Number(base)||0;const commission=s*p/100;return{commission,total:commission+b}},[sales,rate,base]);const money=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">FREE SALES TOOL</p><h1>Commission Calculator</h1><p className="lead">Calculate sales commission from revenue and commission rate, with optional base pay.</p><div className="grid"><div className="panel"><label>Sales amount<div className="inputWrap"><span>$</span><input inputMode="decimal" value={sales} onChange={e=>setSales(e.target.value)}/></div></label><label>Commission rate (%)<input inputMode="decimal" value={rate} onChange={e=>setRate(e.target.value)}/></label><label>Base pay (optional)<div className="inputWrap"><span>$</span><input inputMode="decimal" value={base} onChange={e=>setBase(e.target.value)}/></div></label></div><div className="result"><p className="resultLabel">Commission earned</p><div className="big">{money(r.commission)}</div><div className="resultGrid"><div><span>Sales</span><strong>{money(Number(sales)||0)}</strong></div><div><span>Rate</span><strong>{(Number(rate)||0).toFixed(2)}%</strong></div><div><span>Total incl. base</span><strong>{money(r.total)}</strong></div></div></div></div></section><section className="content"><h2>How sales commission is calculated</h2><p><strong>Commission = Sales Amount × Commission Rate.</strong> Compensation plans can include tiers, quotas and other rules, so use this tool for straightforward percentage-based scenarios.</p><div className="relatedBox"><strong>Related tools</strong><div className="relatedLinks"><Link href="/profit-margin-calculator">Profit Margin Calculator →</Link><Link href="/roi-calculator">ROI Calculator →</Link><Link href="/salary-to-hourly-calculator">Salary to Hourly →</Link></div></div></section></main>}