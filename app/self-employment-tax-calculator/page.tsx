'use client';import Link from'next/link';import{useMemo,useState}from'react';export default function Page(){const[profit,setProfit]=useState('80000'),[incomeTax,setIncomeTax]=useState('18');const r=useMemo(()=>{const p=Math.max(0,+profit||0),net=p*.9235,se=net*.153,deduct=se/2,ordinary=Math.max(0,p-deduct)*(+incomeTax||0)/100;return{net,se,deduct,ordinary,total:se+ordinary}},[profit,incomeTax]);const m=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">US FREELANCER TAX ESTIMATE</p><h1>Self-Employment Tax Calculator</h1><p className="lead">Estimate US self-employment tax and add an optional income-tax planning rate.</p><div className="grid"><div className="panel"><label>Net self-employment profit<input value={profit} onChange={e=>setProfit(e.target.value)}/></label><label>Optional estimated income-tax rate (%)<input value={incomeTax} onChange={e=>setIncomeTax(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Estimated total tax reserve</p><div className="big">{m(r.total)}</div><div className="resultGrid"><div><span>SE tax estimate</span><strong>{m(r.se)}</strong></div><div><span>Half-SE deduction</span><strong>{m(r.deduct)}</strong></div><div><span>Income-tax planning estimate</span><strong>{m(r.ordinary)}</strong></div></div></div></div></section><section className="content"><h2>Planning estimate, not a tax return</h2><p>The self-employment component uses the common 92.35% net-earnings adjustment and 15.3% combined Social Security and Medicare rate. Actual liability can differ because annual wage bases, additional Medicare tax, deductions, filing status, credits and state taxes matter.</p><a className="primary" href="/guides/self-employment-tax-for-freelancers">Read the freelancer tax guide →</a></section></main>}