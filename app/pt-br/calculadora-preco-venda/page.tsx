'use client';import{useMemo,useState}from'react';import Link from'next/link';import{brl}from'../lib/br2026';export default function Page(){const[custo,setCusto]=useState('60'),[margem,setMargem]=useState('40');const r=useMemo(()=>{const c=+custo||0,m=Math.min(99.9,+margem||0)/100,p=c/(1-m);return{p,l:p-c,markup:c?((p-c)/c*100):0}},[custo,margem]);return <main className="shell"><section className="calculator"><p className="eyebrow">PRECIFICAÇÃO</p><h1>Calculadora de Preço de Venda</h1><p className="lead">Calcule o preço de venda necessário para atingir uma margem de lucro desejada.</p><div className="grid"><div className="panel"><label>Custo unitário<div className="inputWrap"><span>R$</span><input value={custo} onChange={e=>setCusto(e.target.value)}/></div></label><label>Margem desejada (%)<input value={margem} onChange={e=>setMargem(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Preço de venda sugerido</p><div className="big">{brl(r.p)}</div><div className="resultGrid"><div><span>Lucro por unidade</span><strong>{brl(r.l)}</strong></div><div><span>Markup equivalente</span><strong>{r.markup.toFixed(2)}%</strong></div></div></div></div></section><section className="content"><h2>Fórmula</h2><p>Preço de venda = custo ÷ (1 − margem desejada). Margem e markup não são a mesma coisa.</p><Link href="/pt-br">← Voltar ao Hub Brasil</Link></section></main>}