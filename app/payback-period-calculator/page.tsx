'use client';import Link from'next/link';import{useMemo,useState}from'react';export default function Page(){const[invest,setInvest]=useState('50000'),[cash,setCash]=useState('5000');const months=useMemo(()=>(+cash||0)>0?(+invest||0)/(+cash||0):0,[invest,cash]);return <main className="shell"><section className="calculator"><p className="eyebrow">INVESTMENT PLANNING</p><h1>Payback Period Calculator</h1><p className="lead">Estimate how long it takes cumulative cash inflows to recover an initial investment.</p><div className="grid"><div className="panel"><label>Initial investment<input value={invest} onChange={e=>setInvest(e.target.value)}/></label><label>Average monthly net cash inflow<input value={cash} onChange={e=>setCash(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Estimated payback period</p><div className="big">{months.toFixed(1)} months</div><div className="resultGrid"><div><span>Years</span><strong>{(months/12).toFixed(2)}</strong></div></div></div></div></section><section className="content"><h2>Simple payback formula</h2><p>Payback period = initial investment ÷ periodic net cash inflow. This simple method does not discount future cash flows.</p><a className="primary" href="/guides/how-to-calculate-payback-period">Read the guide →</a></section></main>}