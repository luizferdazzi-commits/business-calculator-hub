import {notFound} from 'next/navigation';import type{Metadata}from'next';import{getGrowthGuide}from'../../../components/GrowthGuide';import LocalizedGrowthGuidePage from'../../../components/LocalizedGrowthGuidePage';
const map:Record<string,string[]>={'es-mx':['roi-marketing','buen-margen-de-ganancia'],'es-cl':['roi-inversion','margen-pyme'],'de-de':['roi-berechnen','gute-gewinnmarge'],'en-in':['startup-runway-india','freelance-hourly-rate-india'],'en-gb':['break-even-uk-business','good-profit-margin-uk'],'ja-jp':['roi-keisan','break-even-keisan']};
export function generateStaticParams(){return Object.entries(map).flatMap(([locale,topics])=>topics.map(topic=>({locale,topic})))}
export async function generateMetadata({params}:{params:Promise<{locale:string,topic:string}>}):Promise<Metadata>{const{locale,topic}=await params;if(!map[locale]?.includes(topic))return{};const g=getGrowthGuide(`${locale}:${topic}`);if(!g)return{};return{title:g.title,description:g.description,alternates:{canonical:`/${locale}/guides/${topic}`},openGraph:{title:g.title,description:g.description,url:`/${locale}/guides/${topic}`,type:'article'}}}
export default async function Page({params}:{params:Promise<{locale:string,topic:string}>}){const{locale,topic}=await params;if(!map[locale]?.includes(topic))notFound();const g=getGrowthGuide(`${locale}:${topic}`);if(!g)notFound();return <LocalizedGrowthGuidePage guide={g} prefix={`/${locale}/guides`}/>}
