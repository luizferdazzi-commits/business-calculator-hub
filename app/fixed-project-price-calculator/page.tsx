'use client';import Link from'next/link';import{useMemo,useState}from'react';export default function Page(){const[rate,setRate]=useState('75'),[hours,setHours]=useState('40'),[cost,setCost]=useState('250'),[buffer,setBuffer]=useState('20');const r=useMemo(()=>{const base=(+rate||0)*(+hours||0)+(+cost||0),price=base*(1+(+buffer||0)/100);return{base,price,buffer:price-base}},[rate,hours,cost,buffer]);const m=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">FIXED-FEE PRICING</p><h1>Fixed Project Price Calculator</h1><p className="lead">Convert your hourly rate and estimated effort into a fixed project quote with costs and a risk buffer.</p><div className="grid"><div className="panel"><label>Hourly rate<input value={rate} onChange={e=>setRate(e.target.value)}/></label><label>Estimated hours<input value={hours} onChange={e=>setHours(e.target.value)}/></label><label>Direct project costs<input value={cost} onChange={e=>setCost(e.target.value)}/></label><label>Scope/risk buffer (%)<input value={buffer} onChange={e=>setBuffer(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Suggested fixed price</p><div className="big">{m(r.price)}</div><div className="resultGrid"><div><span>Base cost</span><strong>{m(r.base)}</strong></div><div><span>Buffer</span><strong>{m(r.buffer)}</strong></div></div></div></div></section><section className="content"><h2>Hourly rate to fixed fee</h2><p>Estimate the work, include direct costs, then add a buffer for scope risk, revisions and uncertainty.</p><a className="primary" href="/guides/how-to-price-a-fixed-fee-project">Read the pricing guide →</a></section></main>}