'use client';import Link from'next/link';import{useMemo,useState}from'react';
export default function Page(){const[income,setIncome]=useState('90000'),[expenses,setExpenses]=useState('15000'),[tax,setTax]=useState('25'),[hours,setHours]=useState('1600'),[projects,setProjects]=useState('3'),[util,setUtil]=useState('70');const r=useMemo(()=>{const rev=((+income||0)+(+expenses||0))/(1-Math.min(90,+tax||0)/100||1),bill=(+hours||1)*Math.min(100,+util||1)/100,rate=rev/bill;return{rate,rev,bill,perProject:bill/Math.max(1,+projects||1)}},[income,expenses,tax,hours,projects,util]);const m=(n:number)=>n.toLocaleString('en-US',{style:'currency',currency:'USD'});return <main className="shell"><section className="calculator"><p className="eyebrow">FREELANCE PRICING</p><h1>Project Hourly Rate Calculator</h1><p className="lead">Set a sustainable hourly rate when you manage multiple client projects at the same time.</p><div className="grid"><div className="panel"><label>Desired take-home income<input value={income} onChange={e=>setIncome(e.target.value)}/></label><label>Annual business expenses<input value={expenses} onChange={e=>setExpenses(e.target.value)}/></label><label>Estimated tax rate (%)<input value={tax} onChange={e=>setTax(e.target.value)}/></label><label>Available work hours/year<input value={hours} onChange={e=>setHours(e.target.value)}/></label><label>Simultaneous projects<input value={projects} onChange={e=>setProjects(e.target.value)}/></label><label>Billable utilization (%)<input value={util} onChange={e=>setUtil(e.target.value)}/></label></div><div className="result"><p className="resultLabel">Required hourly rate</p><div className="big">{m(r.rate)}</div><div className="resultGrid"><div><span>Revenue target</span><strong>{m(r.rev)}</strong></div><div><span>Billable hours</span><strong>{r.bill.toFixed(0)}</strong></div><div><span>Hours/project capacity</span><strong>{r.perProject.toFixed(0)}</strong></div></div></div></div></section><section className="content"><h2>Pricing across multiple projects</h2><p>Your rate still has to cover income, overhead and taxes even when your attention is divided across clients. Utilization accounts for non-billable work.</p><div className="relatedBox"><strong>Related</strong><div className="relatedLinks"><a href="/guides/pricing-multiple-freelance-projects">Read the guide →</a><a href="/freelance-rate-calculator">Freelance Rate Calculator →</a></div></div></section></main>}